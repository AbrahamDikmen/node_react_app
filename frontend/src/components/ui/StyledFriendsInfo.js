import styled from "styled-components";
import {Container, Box, Input} from "@mui/material";
import {Button} from "@mui/material";

export const FriendsInfoContainer = styled(Container)`
  .image-name {
    .image {
      img {
      }
    }
  }
  .active-user {
  }
  .name {
  }
`;
